[package]
name = "site"
version = "0.1.0"
edition = "2021"

[dependencies]
yew = { version = "^0.20", features = ["csr"] }
yew-router = { version = "^0.17" }

[build-dependencies]
error-chain = "0.12.4"
walkdir = "2.5.0"
